{
  "name": "playwright-aci-tests",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "npx playwright test",
    "test:ci": "npx playwright test --reporter=list"
  },
  "devDependencies": {
    "@playwright/test": "1.55.0",
    "playwright": "1.55.0"
  }
}
